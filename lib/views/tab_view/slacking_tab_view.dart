import 'package:flutter/material.dart';

class SlackingTabView extends StatelessWidget {
  const SlackingTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Slacking'),
      ),
    );
  }
}
